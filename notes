// int recievedId;
        // int return_status = read(cfd, &recievedId, sizeof(recievedId));
        // if (return_status > 0) {
        //   currentPlayer++;
        //   writeToAll(playerSocket, &recievedId, 1);
        //   // write to all player sockets that following 
        //   // read current player's 
        // }else{
        //   // abort
        // }